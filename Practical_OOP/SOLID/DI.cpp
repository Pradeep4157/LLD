#include <bits/stdc++.h>
#define int long long
using namespace std;
/*

    Dependencty Inversion:

    High level modules should not depend on low level modules, instead both of them should depend on interfaces.





*/

signed main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    return 0;
}
